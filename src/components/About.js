import React, { Fragment } from "react";

import Button from "@material-ui/core/Button";

const About = (props) => {
  return (
    <Fragment>
      <Button> hola </Button>
    </Fragment>
  );
};

export default About;
